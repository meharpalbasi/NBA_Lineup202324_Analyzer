# Minimal runtime dependencies for the data pipeline (pipeline/).
# Works on Python 3.9+. The full requirements.txt is a dev freeze
# (jupyter / ipython / streamlit / altair) that requires Python 3.10+ and is
# NOT needed to run the fetchers. The pipeline only imports pandas + nba_api.
nba_api==1.6.1
pandas==2.2.3
# curl_cffi impersonates a real browser's TLS fingerprint. Required since
# ~Feb 2026: stats.nba.com (Akamai) drops plain requests/urllib3 fingerprints.
# See pipeline/nba_http_patch.py. Unpinned upper bound so it resolves on both
# the laptop's Python 3.12 and the mini's 3.9; impersonate="chrome" works in 0.7+.
curl_cffi>=0.7
